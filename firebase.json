{
  "functions": [
    {
      "source": "functions",
      "codebase": "shift-reminders",
      "predeploy": ["npm --prefix \"$RESOURCE_DIR\" run build"],
      "ignore": ["node_modules", ".git", "*.log"]
    }
  ]
}
